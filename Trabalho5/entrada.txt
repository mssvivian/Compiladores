let log = msg => msg asm{println # undefined};

function cria( obj ) {
  return (x) => {
    obj.nome = x; 
    obj.i++;
    return obj; 
  };
}

let o = { i: 0 };
let b = cria( o );

log( b( "Jose" ) );
log( b( "Manoel" ) );